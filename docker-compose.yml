version: '3'
services:
    app:
        build:
            context: ./app/
        volumes: 
            - ./app:/app
        ports:
            - '3000:3000'
        restart: always
    nginx:
        image: nginx:1.15-alpine
        ports:
            - "80:80"
            - "443:443"
        volumes:
            - ./data/nginx:/etc/nginx/conf.d
    certbot:
        image: certbot/certbot